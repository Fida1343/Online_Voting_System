# 🗳️ Online Voting System

A mini-project for demonstrating an **Online Voting System** using:

- **Frontend** → HTML, CSS, JavaScript
- **Backend** → Node.js + Express
- **Database** → MySQL

---

## 📂 Files Included

- `index.html` → Frontend UI
- `server.js` → Backend server (Node.js + Express + MySQL)
- `db.sql` → Database setup script (creates `voting_system` DB with tables)
- `README.md` → Setup guide (this file)

---

## ⚙️ Setup Instructions

### 1. Install Requirements

- Install **Node.js** 👉 [https://nodejs.org](https://nodejs.org)
- Install **XAMPP** 👉 [https://www.apachefriends.org/download.html]  
  (includes MySQL + phpMyAdmin)

---

### 2. Database Setup

1. Start **XAMPP Control Panel** → Start **Apache** and **MySQL**.
2. Open browser → go to 👉 `http://localhost/phpmyadmin`.
3. Create a new database:
   ```sql
   CREATE DATABASE voting_system;
   ```
